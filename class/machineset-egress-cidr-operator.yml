parameters:
  kapitan:
    compile:
      - input_paths:
          - machineset-egress-cidr-operator/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - machineset-egress-cidr-operator/component/main.jsonnet
        input_type: jsonnet
        output_path: machineset-egress-cidr-operator/

